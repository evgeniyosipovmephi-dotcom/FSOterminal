# FSOTerminal — документация проекта

> Версия 2.0.0. Протокол полностью переписан: блочный bulk-ARQ для файлов/голоса/фото
> + лёгкая дорожка MSG для текста (раньше — sliding-window). Подробный дизайн и замеры —
> в `docs/BULK_PROTOCOL_DESIGN.md`.

## Стек
- **Java 21** (BellSoft Liberica Full JDK: `C:\Program Files\BellSoft\LibericaJDK-21-Full`)
- **Gradle 8.7** (wrapper в `gradle/wrapper/`)
- **JavaFX** — входит в Liberica Full (отдельный плагин не нужен)
- **JUnit 5** (`junit-jupiter:5.10.2`)
- **jSerialComm** — работа с COM-портом

## Архитектура протокола

Канал: STM32 — прозрачный мост USB-CDC ↔ FSO-оптика (24000 бод = 2400 байт/с).
Прошивка STM32 **не меняется** — вся логика в Java.

### Формат кадра (FrameCodec)
```
[SOF 0xAA] [SEQ 1B] [TYPE 1B] [LEN 1B] [PAYLOAD = LEN Б] [CRC8 1B]
```
- **SOF = 0xAA** — PtPP2 на FSO-стороне не экранирует 0xAA (экономия байта).
- **CRC8** (poly 0x07) — только для ресинхронизации (найти SOF в мусоре). Целостность
  уже гарантируют PtPP2 CRC32 (на FSO) и аппаратная CRC USB → битый кадр не доходит.
- **MAX_PAYLOAD = 240** (кадр ≤ 245 Б).
- Length-delimited: декодер читает ровно LEN байт, 0xAA в payload не экранируется.

### Две дорожки поверх кадра
| Дорожка | Назначение | Класс |
|---|---|---|
| **bulk** | файлы, голос, фото (блочный ARQ) | `BulkSender` / `BulkReceiver` |
| **MSG** | текст (фрагментация + stop-and-wait) | `MsgChannel` |
| **пейсинг** | один поток отправки, MSG обгоняет файл | `PacedTransmitter` |

### Типы кадров (BulkProtocol, 0x30–0x39)
| Код | Имя | payload |
|---|---|---|
| 0x30 | FILE_BEGIN | `[kind 1][blocks 2][size 4][nameLen 1][name…]` |
| 0x32 | DATA | `[blk 1][idx 2][data ≤56]` — кадр ровно 64 Б |
| 0x33 | BLOCK_END | `[blk 1][count 2]` |
| 0x34 | NACK | `[blk 1][count 2][bitmap: бит=0 → потерян]` |
| 0x35 | BLOCK_DONE | `[blk 1]` |
| 0x36 | FILE_END | `[]` |
| 0x38 | MSG | `[msg_id 1][flag 1][текст ≤57]` (flag 0xFF=ещё / 0x00=последний) |
| 0x39 | MSG_ACK | `[msg_id 1]` |

`kind`: 0=файл, 1=голос, 2=фото. PROBE/PROBE_RESP (0x03/0x04) живут в `FrameCodec`.

### Ключевая идея: кадр 64 байта + over-drive
DATA-кадр = ровно **64 Б** (data 56 + 7 служебных) = один USB-кусок. Это единственный
размер, который можно **передавливать** (слать быстрее физического пола FSO): входной
буфер STM (512 Б) держит 8 таких кадров и сглаживает перегруз, редкие потери чистятся
дешёвыми ретрансмитами. Пейсинг: `delay = пол − overdrive`; для 64 Б пол = 31 мс,
overdrive 11 → **20 мс** (подтверждённый замерами оптимум, ~1700 байт/с).
Кадры ≥128 Б передавливать нельзя — каскадная потеря.

### Блочный ARQ (файлы)
Файл режется на блоки по **400 кадров** (NACK-битмап влезает в 1 кадр). Поток:
`FILE_BEGIN → [DATA…] BLOCK_END → NACK/BLOCK_DONE → (ретрансмит) → след. блок → FILE_END`.
Надёжность управления: отправитель повторяет BLOCK_END по таймауту (ожидание =
`кадров × задержка + RTT`), приёмник отвечает свежим NACK/DONE. Размер блока на скорость
не влияет (доказано замерами) — выбран ради 1-кускового NACK и памяти.

### MSG (текст)
Длинный текст режется на фрагменты ≤57 Б (флаг MORE/LAST), приёмник склеивает БАЙТЫ и
декодирует на LAST (мультибайт не страдает). Stop-and-wait по `msg_id`. MSG обгоняет файл
(`PacedTransmitter.sendNow`), поэтому чат жив во время передачи файла.

### PROBE/живучесть
Каждые `probeIntervalSec` сек — PROBE; разрыв при тишине > `probeIntervalSec × probeMaxMiss`.
Таймаут по `lastReceivedMs` (обновляется на любом входящем байте). RTT в тулбаре.

## Конфигурация (`ProtocolConfig`, Preferences/реестр)
| Ключ | По умолч. | Описание |
|---|---|---|
| `bulkOverdriveMs` | 11 | На сколько мс быстрее пола (0 = безопасно). Задержка кадра = 31−overdrive |
| `probeIntervalSec` | 5 | Интервал PROBE, сек |
| `probeMaxMiss` | 3 | Пропусков PROBE до разрыва |
| `downloadPath` | "" | Папка сохранения (пусто = ~/Downloads) |

Меню **Настройки → Параметры протокола…**. Вступают в силу при следующем подключении.

## Структура исходного кода
```
src/main/java/fsoterminal/
├── protocol/       — чистая Java, без JavaFX (задел на Android)
│   ├── FrameCodec.java        — кодек кадра (SOF/CRC8/Decoder)
│   ├── BulkProtocol.java      — типы, размеры, формула пейсинга
│   ├── PacedTransmitter.java  — поток отправки, приоритет MSG над файлом
│   ├── BulkSender.java        — отправка файла блочным ARQ
│   ├── BulkReceiver.java      — приём файла, NACK/DONE
│   ├── MsgChannel.java        — текст: фрагментация + stop-and-wait
│   └── ProtocolConfig.java    — настройки
├── channel/
│   ├── FSOEmulator.java       — модель потерь (для тестов)
│   └── SerialChannel.java     — COM-порт (jSerialComm)
├── model/ChatItem.java        — запись чата (текст/файл/голос/фото)
├── framesfx/
│   ├── MainWindowSC.java      — контроллер: роутер кадров, UI, настройки, help
│   └── ChatCell.java          — ячейка чата (превью/плеер/прогресс)
├── audio/AudioRecorder.java   — запись голоса (WAV)
└── core/                      — Main + FSOTerminalApp
src/main/resources/fsoterminal/{fxml,css}
```

## Ограничения и поведение прошивки STM32

### FSO-сторона
- Входной/выходной буфер: 512 Б каждый. Лимит на PtPP2-пакет: ≤ 255 Б.
- USB-CDC работает на полной скорости USB (не 115200) — Java-кадр доходит за < 1 мс.

### ⚠️ Упаковка PtPP2: по USB-кускам ≤64 Б (важно!)
USB режет кадр на куски ≤64 Б, и STM пакует **каждый кусок** в отдельный PtPP2-пакет
(+10 Б overhead). Значит **overhead = 10 × ceil(кадр/64)**, а НЕ 10 однократно.
Отсюда:
- кадр должен быть кратен 64 Б, иначе хвостовой огрызок тратит лишний PtPP2-пакет;
- пол передачи растёт ступенями по 64 Б (формула `BulkProtocol.pacingMs` это учитывает);
- для кадра 245 Б реальный пол ≈ 119 мс (285 FSO-байт), а не 106 — поэтому 64-Б кадр оптимален.

### Диагностика
- PROBE RTT растёт > 2× во время передачи → переполнение буфера STM32.
- «Прерванные передачи на блоках» → таймаут ожидания ответа меньше времени слива очереди.

## Производительность (02.06.2026, COM22↔COM25, кадр 64 Б, over-drive 11 = задержка 20 мс)
| Направление | Объём | Скорость | % от теор. 2400 |
|---|---|---|---|
| A→B | 100 КБ | ~1672 байт/с | 69.6% |
| B→A | 100 КБ | ~1708 байт/с | 71.2% |

Потолок для 64-Б кадра ≈ 1849 байт/с (over-drive даёт ~90% от него). Полный разбор
подбора кадра/задержки и размера блока — в `docs/BULK_PROTOCOL_DESIGN.md`.

## Тесты
- **Синтетические (без железа):** `gradlew test` — `BulkProtocolSyntheticTest` (bulk+MSG
  на эмуляторе с потерями), `FrameCodecTest`, `FrameCodecStressTest`, `FSOEmulatorTest`.
- **Интеграционные (реальные COM):** `SerialLoopbackTest` — `file_AtoB/BtoA`, `msg_AtoB/BtoA`.
  Включаются `-Dserial.test.enabled=true`. Детали и параметры — в `TESTS.md`.

## Сборка и запуск
```bat
set JAVA_HOME=C:\Program Files\BellSoft\LibericaJDK-21-Full
gradlew.bat test        :: тесты
gradlew.bat run         :: запуск приложения
gradlew.bat jpackage    :: portable .exe
```

## Частые ошибки (чтобы не повторять)
- **Таймаут ожидания ответа должен учитывать время слива очереди пейсинга**, а не быть
  фиксированным — иначе отправитель сдаётся раньше, чем блок уходит в канал.
- **Over-drive безопасен только для 1-кускового кадра (≤64 Б).** Для больших кадров
  (≥128 Б) задержку ниже пола ставить нельзя — каскадная потеря.
- Модель упаковки PtPP2 — **по 64-байтным USB-кускам**, не «10 Б на логический кадр».
